
import Header from "@/components/Header";
import Logo from "@/components/ui/Logo";

const CustomHeader = () => {
  return (
    <Header 
      logo={<Logo />}
    />
  );
};

export default CustomHeader;
